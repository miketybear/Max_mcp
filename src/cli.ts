#!/usr/bin/env node

/**
 * CLI entry point for maximo-mcp-server
 *
 * This file serves as the bin entry point when the package is installed globally
 * or executed via npx. It simply delegates to the main server entry point.
 */

import './index.js';
